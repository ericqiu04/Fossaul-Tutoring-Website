//scrapped idea, maybe later on i add
import React from 'react';
import {Button} from 'react-bootstrap';
function Title(){
    return(

        <div className = "title">
            <Button variant = "dark" href = "classes.html">Classes</Button>
            <img src = "title.jpg" alt = "img"></img>
        </div>

    );
}

export default Title;